# Build-time dependencies for the agri-chatbot (ADTC 2026, Agriculture track)
#
# The project is llama.cpp-only at all phases. The embedding index is built
# with the GGUF all-MiniLM-L6-v2 model through llama-cpp-python, so torch and
# sentence-transformers are NOT required anymore.
#
# Runtime: scikit-learn, llama-cpp-python, numpy, python-docx (see requirements.txt)

scikit-learn>=1.2
llama-cpp-python>=0.2.88
numpy>=1.24
python-docx>=1.0
