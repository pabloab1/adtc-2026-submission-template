# RUNTIME dependencies — chat-time only.
# Retrieval runs over the pre-built index (index/chunk_vectors.npy) with a
# pure-numpy embedding of the incoming question; no torch, no transformers.
scikit-learn>=1.3      # legacy fallback path (retrieval.py)
llama-cpp-python>=0.2.0  # on-device LLM (llama.cpp)
numpy>=1.24            # chunk retrieval vectors + runtime MiniLM embedder
python-docx>=1.0       # only for build_index; harmlessly importable at runtime
streamlit>=1.30        # local web UI (app.py) — not part of ADTC submission
